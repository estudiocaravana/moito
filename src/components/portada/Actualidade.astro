---
import actualidade from "../../data/actualidade.json";
---

<div class="caixa mt-36">
  <h2 class="text-4xl">Actualidade</h2>
  <div class="mt-3">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Vel corrupti laudantium similique quam fugit. Minima alias repellendus.</div>
  <div class="md:grid md:grid-cols-2 grid xl:grid-cols-3 gap-12 mt-12">
    {
      actualidade.novas.map((nova) => (
        <a href="#" class="hover:text-cyan-500">
          <div class="aspect-raiz shadow-md relative overflow-hidden">
            <img
              class="object-cover min-h-full min-w-full absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2"
              src={`/src/assets/actualidade/${nova.imaxe}.jpg`}
              alt="A brown dog sitting on a grassy field under a clear sky, looking attentively forward, surrounded by trees in the background, calm and peaceful atmosphere"
            />
          </div>
          <div class="font-bold mt-5">{nova.tit}</div>
          <div class="text-base mt-3">{nova.resumen}</div>
          <div class="flex mt-4 text-base gap-3">
            <div class="text-cyan-500 font-bold">{nova.autor}</div>
            <div>&middot;</div>
            <div class="">{nova.data}</div>
          </div>
        </a>
      ))
    }
  </div>
  <a href="#" class="font-bold block hover:text-slate-600 mt-12"
    >Toda a actualidade do libro en galego no noso Blog
    <svg class="size-5 inline-block" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"></path></svg>
  </a>
</div>
